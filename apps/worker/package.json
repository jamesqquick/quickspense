{
  "name": "@quickspense/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --persist-to=../../.wrangler-shared",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy --dry-run --outdir=dist && wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@quickspense/domain": "workspace:*",
    "agents": "^0.11.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
